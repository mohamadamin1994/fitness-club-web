<!DOCTYPE html>
<html lang="fa">
<head>
  <meta charset="UTF-8">
  <title>باشگاه بدنسازی | FitnessClub</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div class="welcome">
    <div id="clock" class="clock"></div>
    <h1>خوش آمدید به باشگاه بدنسازی 💪</h1>
    <p>با برنامه‌های تمرینی و غذایی حرفه‌ای، به فرم دلخواهتان برسید!</p>
    <button onclick="enterApp()">شروع کنید</button>
  </div>

  <div class="app" style="display:none;">
    <nav>
      <ul>
        <li onclick="showPage('training')">برنامه تمرینی</li>
        <li onclick="showPage('nutrition')">برنامه غذایی</li>
        <li onclick="showPage('exercises')">آموزش حرکات</li>
        <li onclick="showPage('about')">درباره باشگاه</li>
        <li onclick="showPage('contact')">تماس با ما</li>
      </ul>
    </nav>

    <section id="training" class="page">اینجا برنامه تمرینی نمایش داده می‌شود...</section>
    <section id="nutrition" class="page" style="display:none;">اینجا برنامه غذایی نمایش داده می‌شود...</section>
    <section id="exercises" class="page" style="display:none;">آموزش حرکات ورزشی...</section>
    <section id="about" class="page" style="display:none;">معرفی باشگاه و مربیان...</section>
    <section id="contact" class="page" style="display:none;">فرم تماس با باشگاه...</section>
  </div>

  <script src="script.js"></script>
</body>
</html>

body {
  font-family: 'Vazir', sans-serif;
  background: linear-gradient(to right, #f44336, #212121);
  color: #fff;
  text-align: center;
  margin: 0;
  padding: 0;
}

.welcome {
  padding: 50px 20px;
}

.clock {
  font-size: 24px;
  margin-bottom: 20px;
  color: #ffeb3b;
}

button {
  background-color: #ff5722;
  color: white;
  border: none;
  padding: 10px 20px;
  font-size: 18px;
  border-radius: 8px;
  cursor: pointer;
}

nav ul {
  list-style: none;
  display: flex;
  justify-content: center;
  gap: 15px;
  padding: 20px;
  background-color: #333;
}

nav li {
  background-color: #e53935;
  padding: 10px 15px;
  border-radius: 6px;
  cursor: pointer;
}

.page {
  padding: 30px;
}

function updateClock() {
  const now = new Date();
  const clock = document.getElementById('clock');
  clock.textContent = now.toLocaleTimeString('fa-IR');
}
setInterval(updateClock, 1000);
updateClock();

function enterApp() {
  document.querySelector('.welcome').style.display = 'none';
  document.querySelector('.app').style.display = 'block';
}

function showPage(pageId) {
  document.querySelectorAll('.page').forEach(page => {
    page.style.display = 'none';
  });
  document.getElementById(pageId).style.display = 'block';
}
